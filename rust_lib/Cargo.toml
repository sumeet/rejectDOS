[package]
name = "rust_lib"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[profile.release]
opt-level = "s"      # Optimize for size
panic = "abort"      # Prevent exception frame landing padding
lto = true           # Link Time Optimization to make it super small
